# 构建安装包指南

这份文档对应当前仓库里的 `scripts/build_packages.sh`，以原生 roothide 发布线为准。

更完整的 roothide 维护者说明见 [docs/roothide-packaging.md](docs/roothide-packaging.md)。

## 1. 产物命名

所有发布产物统一输出到仓库根目录的 `out/`：

- TrollStore: `out/ChargeLimiter_<VERSION>_TrollStore.tipa`
- rootful: `out/ChargeLimiter_<VERSION>_rootful_arm.deb`
- rootless: `out/ChargeLimiter_<VERSION>_rootless_arm64.deb`
- roothide: `out/ChargeLimiter_<VERSION>_roothide_arm64e.deb`

**roothide 默认走原生构建**：Xcode scheme `ChargeLimiter roothide` + 模板 `ChargeLimiter/Package_roothide/`（`THEOS_PACKAGE_SCHEME=roothide`）。  
**不再**把「从 rootless 转换」当作正式默认路径；转换仅作紧急回滚（`--legacy-roothide-convert`）。

默认情况下，`<VERSION>` 会自动读取 `ChargeLimiter.xcodeproj/project.pbxproj` 里的 `MARKETING_VERSION`。

如果你需要临时指定版本号，也可以直接传参：

```bash
./scripts/build_packages.sh 1.14.0
```

## 2. 必备依赖

- Xcode
- Command Line Tools
- `xcodebuild`
- `dpkg-deb`
- `zip`
- `ldid`
- `plutil`
- `xcrun`
- Theos headers（常见：`/opt/theos/vendor/include`，并链到 `/opt/include`）
- **原生 roothide 额外需要 libroothide / roothide libroot**
  - `/opt/theos/vendor/lib/iphone/roothide`（供 `-lroot`）
  - `/opt/theos/vendor/include/roothide`
  - 推荐安装 [roothide/theos](https://github.com/roothide/theos) 或 [libroothide Xcode devkit](https://github.com/roothide/libroothide/releases)

如果本机没有 `dpkg-deb`，可以先安装：

```bash
brew install dpkg
```

缺少 roothide 库时，默认构建会在链接 `ChargeLimiter roothide` 时失败。可临时：

```bash
./scripts/build_packages.sh --skip-roothide
# 或紧急：
./scripts/build_packages.sh --legacy-roothide-convert
```

正式发版应补齐工具链，使用原生默认路径。

## 3. 推荐构建方式

在仓库根目录执行：

```bash
./scripts/build_packages.sh
```

脚本会自动完成这些事：

1. 分别构建 `ChargeLimiter`（rootful）、`ChargeLimiter rootless`、`ChargeLimiter roothide`（原生）
2. 对主程序和 daemon 做 `strip`
3. 重新签名（roothide 会合并 `scripts/roothide.entitlements`）
4. 生成 `.tipa`、`rootful .deb`、`rootless .deb`、`roothide .deb`
5. 校验 bundle id、架构、deb 包头、安装根和宿主系统元数据
6. roothide 额外校验：`Architecture: iphoneos-arm64e`、包内为 `Applications/` 布局、无 `/var/jb` 安装前缀

## 4. 当前脚本的关键约定

1. 不再把构建产物直接拷进仓库里的 `Package` / `Package_rootless` / `Package_roothide`  
   说明：脚本会先复制一份临时打包树到系统临时目录，再在那里写入 `.app` 和 `DEBIAN/control` 版本号。

2. **roothide 默认原生**  
   说明：使用 scheme `ChargeLimiter roothide` 与 `Package_roothide`。legacy 转换默认关闭。

3. 版本号不硬编码在脚本里  
   说明：不传参数时读取工程 `MARKETING_VERSION`，与 App `CFBundleShortVersionString`（`$(MARKETING_VERSION)`）对齐。

4. `xcodebuild` 默认不会再自动触发 MonkeyDev 打包/安装 phase  
   说明：`MonkeyDevBuildPackageOnAnyBuild` / `MonkeyDevInstallOnAnyBuild` 默认为 `NO`。

## 5. 常见命令

### 5.1 标准发布构建（含原生 roothide）

```bash
./scripts/build_packages.sh
```

### 5.2 指定版本号构建

```bash
./scripts/build_packages.sh 1.14.0
```

### 5.3 跳过 roothide

```bash
./scripts/build_packages.sh --skip-roothide
```

也可以配合版本号：

```bash
./scripts/build_packages.sh 1.14.0 --skip-roothide
```

### 5.4 紧急：legacy rootless→roothide 转换

```bash
./scripts/build_packages.sh --legacy-roothide-convert
```

仅用于工具链不齐或对比回归；**不是**正式发布默认。

### 5.5 只想本地编译，不想打包

```bash
xcodebuild \
  -project ChargeLimiter.xcodeproj \
  -scheme "ChargeLimiter" \
  -destination "generic/platform=iOS" \
  -configuration Release \
  -derivedDataPath build_rootful \
  CODE_SIGNING_ALLOWED=NO \
  ARCHS=arm64
```

rootless：

```bash
xcodebuild \
  -project ChargeLimiter.xcodeproj \
  -scheme "ChargeLimiter rootless" \
  -destination "generic/platform=iOS" \
  -configuration Release \
  -derivedDataPath build_rootless \
  CODE_SIGNING_ALLOWED=NO \
  ARCHS=arm64
```

roothide（需 libroothide / roothide libroot）：

```bash
xcodebuild \
  -project ChargeLimiter.xcodeproj \
  -scheme "ChargeLimiter roothide" \
  -destination "generic/platform=iOS" \
  -configuration Release \
  -derivedDataPath build_roothide \
  CODE_SIGNING_ALLOWED=NO \
  ARCHS=arm64 \
  THEOS_PACKAGE_SCHEME=roothide
```

如果后面又想恢复 MonkeyDev 的自动 phase，可以在命令里显式打开：

```bash
MonkeyDevBuildPackageOnAnyBuild=YES MonkeyDevInstallOnAnyBuild=YES xcodebuild ...
```

## 6. 常见问题

- `Build output app not found`
  - 说明：Xcode 构建没有成功，先看 scheme、SDK 和依赖是否齐全。

- `Missing command: dpkg-deb` / `ldid`
  - 说明：本机缺少打包依赖，按第 2 节先装好再跑。

- `ld: library 'root' not found`（roothide）
  - 说明：缺少 `/opt/theos/vendor/lib/iphone/roothide` 下的 libroot。安装 roothide/theos 或 libroothide devkit；或临时 `--skip-roothide` / `--legacy-roothide-convert`。

- `application-identifier mismatch`
  - 说明：签名或 bundle id 不一致，优先检查 entitlements 和 `Info.plist`。

- 直接 `xcodebuild` 时还有 MonkeyDev 上传或安装动作
  - 说明：大概率是命令里手动传了 `MonkeyDevBuildPackageOnAnyBuild=YES` 或 `MonkeyDevInstallOnAnyBuild=YES`。

- 构建日志提示 legacy conversion
  - 说明：只有使用了 `--legacy-roothide-convert`。默认应看到 native scheme + `Package_roothide`。

- 构建能过，但链接 `libdobby.a` 失败
  - 说明：`libdobby.a` 仍依赖本机 MonkeyDev / Theos 环境，请确认对应静态库存在。

## 7. 发版前检查

建议每次发版前至少确认：

1. `MARKETING_VERSION` 已更新
2. `CHANGELOG.md` 已同步
3. `Readme.md` 的功能说明和当前 UI 一致
4. `out/` 里的 TrollStore、rootful、rootless、roothide 产物都重新生成
5. roothide deb：`Architecture: iphoneos-arm64e`、无 `var/jb`、Info 短版本 = 发版版本
6. 真机至少验证一次安装、打开、设置保存与基础充电控制（roothide 见 [docs/roothide-packaging.md](docs/roothide-packaging.md) §7）
