# 构建安装包指南

这份文档对应当前仓库里的 `scripts/build_packages.sh`，以 `2026-03-19` 之后的脚本行为为准。

## 1. 产物命名

所有发布产物统一输出到仓库根目录的 `out/`：

- TrollStore: `out/ChargeLimiter_<VERSION>_TrollStore.tipa`
- rootless: `out/ChargeLimiter_<VERSION>_rootless_arm64.deb`
- roothide: `out/ChargeLimiter_<VERSION>_roothide_arm64e.deb`

默认情况下，`<VERSION>` 会自动读取 `ChargeLimiter.xcodeproj/project.pbxproj` 里的 `MARKETING_VERSION`。

如果你需要临时指定版本号，也可以直接传参：

```bash
./scripts/build_packages.sh 1.11.1
```

## 2. 必备依赖

- Xcode
- Command Line Tools
- `xcodebuild`
- `dpkg-deb`
- `zip`
- `ldid`
- `plutil`
- `xcrun`

如果本机没有 `dpkg-deb`，可以先安装：

```bash
brew install dpkg
```

## 3. 推荐构建方式

在仓库根目录执行：

```bash
./scripts/build_packages.sh
```

脚本会自动完成这些事：

1. 分别构建 `ChargeLimiter rootless` 和 `ChargeLimiter roothide`
2. 对主程序和 daemon 做 `strip`
3. 重新签名 `.app`、主程序和 daemon
4. 生成 `.tipa`、`rootless .deb`、`roothide .deb`
5. 校验 `CFBundleIdentifier`、二进制架构和 TrollStore 不允许的 entitlement

## 4. 当前脚本的关键改动

和旧流程相比，现在有几个重要变化：

1. 不再把构建产物直接拷进仓库里的 `Package_rootless` / `Package_roothide`
   说明：
   脚本会先复制一份临时打包树到系统临时目录，再在那里写入 `.app` 和 `DEBIAN/control` 版本号。

2. 不再把版本号硬编码在脚本里
   说明：
   不传参数时会自动读取工程版本，减少发版时忘记同步的问题。

3. `xcodebuild` 默认不会再自动触发 MonkeyDev 打包/安装 phase
   说明：
   工程里的 `MonkeyDevBuildPackageOnAnyBuild` 和 `MonkeyDevInstallOnAnyBuild` 默认值已经改成 `NO`，直接构建时日志会干净很多。

## 5. 常见命令

### 5.1 标准发布构建

```bash
./scripts/build_packages.sh
```

### 5.2 指定版本号构建

```bash
./scripts/build_packages.sh 1.10.1
```

### 5.3 只想本地编译，不想打包

```bash
xcodebuild \
  -project ChargeLimiter.xcodeproj \
  -scheme "ChargeLimiter rootless" \
  -destination "generic/platform=iOS" \
  -configuration Release \
  -derivedDataPath build_rootless \
  CODE_SIGNING_ALLOWED=NO \
  ARCHS=arm64
```

如果你后面又想恢复 MonkeyDev 的自动 phase，可以在命令里显式打开：

```bash
MonkeyDevBuildPackageOnAnyBuild=YES MonkeyDevInstallOnAnyBuild=YES xcodebuild ...
```

## 6. 常见问题

- `Build output app not found`
  - 说明：Xcode 构建没有成功，先看 scheme、SDK 和依赖是否齐全。

- `Missing command: dpkg-deb` / `ldid`
  - 说明：本机缺少打包依赖，按第 2 节先装好再跑。

- `application-identifier mismatch`
  - 说明：签名或 bundle id 不一致，优先检查 entitlements 和 `Info.plist`。

- 直接 `xcodebuild` 时还有 MonkeyDev 上传或安装动作
  - 说明：大概率是命令里手动传了 `MonkeyDevBuildPackageOnAnyBuild=YES` 或 `MonkeyDevInstallOnAnyBuild=YES`。

- 构建能过，但链接 `libdobby.a` 失败
  - 说明：工程虽然已经去掉 `/opt/lib` 搜索路径警告，但 `libdobby.a` 的文件引用仍然指向你的本机 MonkeyDev / Theos 环境，请确认对应静态库实际存在。

## 7. 发版前检查

建议每次发版前至少确认：

1. `MARKETING_VERSION` 已更新
2. `ChangeLog.md` 已同步
3. `Readme.md` 的功能说明和当前 UI 一致
4. `out/` 里的三个产物都重新生成
5. 真机至少验证一次安装、打开、查看策略诊断和基础充电控制
