1.11.1版本更新日志:

已完成:
* 修复主界面供电状态误判：当设备实际在放电、并未真正由外部供电时，不再因为残留适配器信息或阈值状态把主状态错误显示成“已连接电源 · 停止充电”。
* 主界面顶部状态、电池动画和适配器卡片现在统一按“实际供电路径”判断，优先依据 `IsCharging`、实时电流和 `ExternalConnected`，减少三处状态互相打架的情况。
* 主界面“守护策略”入口文案调整为“供电状态”，避免把 daemon 的策略状态和当前实际供电路径混为一谈。
* 调整关键配置项后会立即重新评估策略状态，不必再等待下一次偶发电池事件才刷新显示。
* 继续保留插电保持、温控暂停、禁流、补电等策略细节，但把这些细节和主界面的实时供电显示解耦，减少误导。

仍需验证 / 后续:
* 插电保持相关过渡状态目前仍会受实时电流变化影响，改阈值后短暂出现“正在充电”再回到“插电保持中”通常属于状态收敛过程，仍建议继续观察不同机型上的持续时间。
* 高级页里的策略诊断仍然保留 raw daemon 状态，用于排查后台逻辑；如果后续用户继续反馈“文案理解成本高”，可以再考虑补充更明确的过渡说明。

1.11.0版本更新日志:

已完成:
* 右上角电池状态图标重做为更生动的原生动画版本：会按充电、低电量、停充保持、补电、温控暂停、禁止进流等不同策略状态切换颜色、图标和动画表现。
* 电池状态视图新增连续流光、低电量脉冲、高温发热光效等状态动画，并兼容“减少动态效果”场景，在无障碍或非可见窗口下自动降级为静态表现。
* 主界面电池状态展示改为直接基于 daemon 策略状态驱动，不再只是简单显示百分比和是否充电。
* 保留模拟器独立测试入口，但回退了会影响真机的动态私有符号加载改动，恢复 `MGCopyAnswer`、`BKSHIDEventRegisterEventCallback`、`UIApplicationInstantiateSingleton` 等真机原始调用路径。
* 恢复工程里 `BackBoardServices`、`GraphicsServices`、`libMobileGestalt`、`IOKit.tbd` 的原有链接方式，减少真机读取电池信息失败和配置加载变慢的回归风险。

仍需验证 / 后续:
* 新电池动画的视觉节奏、颜色层次和状态映射仍需要继续收集不同机型的真机反馈。
* 真机侧电池信息读取和配置加载已按最小影响原则恢复原路径，但仍建议继续做几轮长时间稳定性观察。

1.10.1版本更新日志:

已完成:
* 新增“插电保持”模式：当电量达到停止充电目标后，不再继续给电池充电；当电量回落到目标下方小范围时，再自动恢复补电，整体体验更接近电脑插电驻留。
* daemon 增加 hold 周期检查与策略状态输出，主页面现在可以区分“插电保持中 / 插电保持中·补电 / 已连接电源·停止充电 / 温控暂停充电 / 停充时已禁流”等不同状态。
* 新增“保持带宽”设置，可控制目标电量下方多少范围内允许自动补电，例如目标 50%、带宽 2% 时，大致会在 48% - 50% 间工作。
* 新增“保持策略”预设：平衡 / 偏向外接供电 / 偏向减少循环 / 智能自适应。不同预设会影响 hold 轮询频率，以及持续放电时是否提前补电；智能自适应会根据平均电流、适配器功率、无线充和临近温控阈值在 runtime 自动切换当前生效策略。
* 新增“系统优化充电”协调：支持永久停用系统优化充电，也支持仅在保持/停充阶段临时停用，退出后再尝试恢复；现在还会记录接管前原始状态、协调会话，并在 daemon 重启后先按真实电池快照重算，再决定恢复还是继续接管。
* 新增“满充计划”：支持每隔 N 天在指定时间开启一次满充窗口，窗口内临时解除电量上限，但温度控制仍然保留。
* 满充计划改为按单次边界定时触发，支持时间变化后自动重算，关闭后不再保留额外后台轮询。
* 主页面新增“电源路径/策略状态”信息展示，可直接看到守护策略、充电命令、系统停充抑制、系统优化充电状态、保持策略与保持范围。
* 新增“策略诊断”页面，可集中查看当前策略原因、最近一次策略切换时间与原因、hold 持续放电计数、轮询间隔、提前补电辅助状态、当前实时信号、Smart Charge 接管状态，以及最近若干次策略切换和长时间策略事件时间线。
* 策略事件已持久化到独立 `sqlite` 事件表，并接入历史页“策略事件时间线”；支持按全部 / 状态 / 保持 / 接管 / 温控筛选，并支持导出事件时间线、复制诊断摘要、复制长测校准模板。
* 修复“充电高级”页面在手机上的布局显示问题：长标题、长副标题和右侧值现在会根据屏幕宽度自适应换行，不再容易挤压、重叠或被裁掉。

仍需验证 / 后续:
* 还没有做到真正意义上的硬件旁路充电。目前仍然是软件层控制“是否继续充电”和“何时恢复补电”，并不等同于硬件级 bypass。
* 智能自适应和保持阈值已经有第一版实现，但不同机型、不同适配器功率、无线 / 有线和温控环境下，默认阈值仍需要更多真机校准。
* Smart Charge 协调已经可用，但不同设备、不同系统版本下的实际表现仍需要更多真机长时间验证。

1.10更新日志:
* 新增“满充计划”，支持每隔 N 天在指定时间开启一次满充窗口
* 满充窗口内仅解除电量上限，温度控制仍然保留
* 支持设置开始时间与持续小时数，daemon 会在后台自动切换并立即应用策略
* 满充计划改为单次边界定时，支持时间变化后自动重算，关闭时不保留额外轮询


1.9.6.3版本更新日志:
* 停止充电电量设为 100% 时，电量控制交由系统接管，不再按电量阈值主动断充
* 保留温度控制与安全兜底策略，100% 模式下仍可按温控逻辑介入
* 新增“系统电量控制中，温度控制仍生效”浮层提示，仅在切换到 100% 时短暂显示并自动隐藏
* 停充电量100，图标不显示绿条

1.9.6.2版本更新日志:
* 修复越狱版的温度配置保存，优化一些逻辑

1.9.6.1版本更新日志:
* 修复巨魔配置保存，统一颜色背景

1.9.6版本更新日志:
* 修复 iPad 上点击操作菜单闪退的问题
* 优化 iPad 界面布局，支持居中显示与分屏适配

1.9.5版本更新日志:
* 增加迁移/删除旧版数据功能，迁移数据后会自动删除旧版数据
* 配置文件目录输出在应用数据目录中，删除软件不会保留数据

1.9.3版本更新日志:
* 顶部“ChargeLimiter”右侧新增刷新按钮，标题基线对齐
* 刷新按钮改为调用 daemon 的 apply_now 立即执行策略
* daemon 充电策略抽成统一函数，便于复用与维护

1.9.2版本更新日志:
* UI和交互的优化

1.9.0版本更新日志:
* 包名/标识更新为 com.chargelimiter.mod（避免与原作者冲突）
* 完整补齐本地化资源（Base / 英文 / 简体中文），语言切换逻辑修复
* 充电与温控设置交互优化：滑块拖动震动反馈，支持震动强度分级
* 设置页结构与样式优化（入口卡片统一风格、说明文字间距调整）
* 历史统计图表细节优化（Tooltip、时间轴标签、自适应密度、选中点高亮）
* iOS 15 兼容图标回退处理，图标语义与颜色规范统一
* 修复温度控制开关进入页面偶发灰色的问题
* 配置文件保存位置优化，软件设置里面有一键跳转对应文件夹功能，需安装Filza。

1.8.0版本更新日志:
* 使用原生 UIKit 界面替代 WebView，提升性能和稳定性
* 新增温度边缘触发模式（高温停充 + 降温恢复）
* 移除悬浮窗功能
* 修复多个 Bug，包括配置持久化、滑块验证等
* 最低支持 iOS 13.0
* 优化代码结构，移除冗余文件
