# 任务清单：电池兼容性测试页面

## 1. 页面骨架与入口

- [x] 1.1 新建 `CLBatteryCompatibilityTestViewController`（.h/.m），实现页面骨架：说明区、三项测试选择区、开始/取消按钮、进度区、结果区、探针按钮区，并加入 Xcode 工程各 target；验证：rootful 方案编译通过，页面可从入口推入显示
- [x] 1.2 主页面"更多功能"区在历史统计入口下方新增"电池兼容性测试"入口卡片与跳转方法（复用现有入口卡片视觉模式）；验证：编译通过，真机/模拟器上入口位于历史统计下方且点击进入新页面

## 2. 测试编排核心

- [x] 2.1 实现前置检查（插电、daemon 在线、电量范围）与提示；验证：未插电时点击开始被阻止并显示对应提示
- [x] 2.2 实现配置快照/恢复器：测试前保存全局开关、智能停充等受影响配置，完成/取消/离开页面三路径恢复，残留快照下次进入检测提示；验证：任一退出路径后配置恢复为测试前值
- [x] 2.3 实现测试状态机与三项测试逻辑（停充/智能停充/禁流）：独立 1 秒轮询采样、120 秒上限、滑动窗口 ≥5mA 持续电流判定、状态变化早停；验证：插电真机上停充测试能在状态变化后早停并给出正确结论
- [x] 2.4 实现单项测试选择（默认三项全选）；验证：仅勾选停充测试时只执行该项

## 3. 进度与结果展示

- [x] 3.1 实现实时进度区：当前测试项、已用时/预计剩余、实时电池电流、状态变化事件；验证：测试中页面每秒刷新电流与进度
- [x] 3.2 实现结果区：分项结论、监测期最大/最低电流、总体判定（双不支持时提示设备不被 CL 支持）；验证：跑完三项后结果正确展示且与判定规则一致

## 4. 探针入口与本地化

- [x] 4.1 实现"运行停充控制探针"按钮：复用 `runChargeControlProbe` API，运行中防重复触发，完成后展示结论摘要；验证：点击后运行并显示探针结论
- [x] 4.2 补全 en.lproj / zh-Hans.lproj 双语文案；验证：切换语言后页面文案正确跟随

## 5. 验证

- [x] 5.1 rootful + rootless 两方案 xcodebuild 编译通过；验证：`xcodebuild -project ChargeLimiter.xcodeproj -scheme "ChargeLimiter" ...` 与 rootless scheme 均成功
- [x] 5.2 打包冒烟已完成（`./scripts/build_packages.sh` 四类包产出成功，含审查修复后复跑）。真机冒烟**已记录为延期**：用户暂不可用，该项移交验证阶段作为开放项（见下方"开放验证项"）

## 6. 代码审查遗留（review_mode: standard，已接受项）

- [x] 6.1 审查结论：Ready to merge（C1/C2/I1-I5/N1/N2/N3 全部修复并经复核确认；rootful/rootless 编译与打包复验通过）
- [x] 6.2 已接受偏差（低风险，随真机冒烟观察）：
  - N1 残留：abort 排水上限 5s，若写悬挂至 URLSession 60s 超时才落地仍可能覆盖恢复（localhost 写秒回或连接失败，有界折衷）
  - writeSnapshot 脏快照守卫命中时弹窗文案为"无法读取当前配置"，与实际原因不符（主守卫存在时几乎不可达，纯文案瑕疵）
  - M8（既有行为，非本次引入）：CLAPIClient.setConfigWithKey 传输失败也会持久化本地镜像，存在本地/daemon 配置漂移隐患，本流程放大触发面，建议另行评估
  - 既有亚秒窗口：弹窗"丢弃"清快照后重启测试理论上可捕获脏配置（precheck"正在充电"要求使其几乎不可达）
- [x] 6.3 真机冒烟处置：因用户暂不可用，显式记录为延期开放项移交验证/归档阶段跟踪（非已执行）；归档前最终确认时须再次向用户确认

## 开放验证项（OPEN — 验证阶段必须列入验证报告）

- **真机冒烟（未执行）**：安装 out/ChargeLimiter_1.15.3_TrollStore.tipa（或对应 deb）后完整验证——入口位于历史统计下方、完整一键测试三项、状态快速变化时早停、单项重测、取消/返回恢复配置、探针按钮、中英切换、测试中强杀 App 后残留快照恢复提示。三个审查 Critical（testDone 接线/取消死锁）均属真机首跑即暴露的类型，该项为合并前强建议项。
