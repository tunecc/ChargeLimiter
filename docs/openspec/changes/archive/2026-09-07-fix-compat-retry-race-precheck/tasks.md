# 任务清单：修复禁流重试竞态误判与前置检查"正在充电"行误导

## 1. 引擎

- [x] 1.1 禁流确认窗口状态机：窗口自「写生效」样本起算（首个非充电样本开窗并初始化判定窗口统计）；恢复事件仅在窗口已建立时计数；重试写完成仅清 in-flight 标志、不重置窗口；验证：代码复核 + rootful 编译
- [x] 1.2 生效观察上限：窗口未建立时累计连续充电采样（重试写在途暂停计数），达到确认窗口长度（10 个）→ 按恢复事件处理（重下发或耗尽判定）；验证：代码复核 + rootful 编译

## 2. 展示与文案

- [x] 2.1 前置检查移除「正在充电」行（含 runPrecheck 的 charging 项、失败文案、橙色特判）；说明区删除前置检查时刻 tip；「请在插电且正在充电时运行」改「请在插电状态下运行」；验证：代码复核 + plutil + 编译
- [x] 2.2 双语文案同步（删 2 个失效 key、改 1 个 tip key；「正在充电」key 其他页面共用保留）；验证：plutil + 编译

## 3. 验证

- [x] 3.1 rootful + rootless 编译、打包冒烟；真机复测由用户执行（重点：仅测禁流单项应判「支持」、重试事件不再秒级连跳、前置检查无「正在充电」行）
