# 任务清单：修复禁流测试信号漏检与系统对抗误判

## 1. 引擎

- [x] 1.1 禁流信号集恢复 IsCharging 翻转（基线锚定）：`changed = (bCharging && !isCharging) || (bExtConnected && !extConnected) || (bCurrent >= 0 && current < 0)`；验证：代码复核 + rootful 编译
- [x] 1.2 禁流对抗重试：确认窗口内检测到充电恢复（isCharging && 电流≥5mA）时重新下发禁流（≤3 次、防重入、in-flight 计数），重置确认窗口；重试耗尽判"禁流无法维持"；验证：代码复核 + rootful 编译

## 2. 展示与文案

- [x] 2.1 结果统计口径：引擎另记确认窗口最大/最低电流，verdict 事件优先携带确认窗口统计（无状态变化时用全程统计），结果卡文案改"判定窗口电流"；验证：代码复核 + 编译
- [x] 2.2 双语文案同步（重试提示、禁流无法维持、判定窗口文案、前置检查说明提示）；验证：plutil + 编译

## 3. 验证

- [x] 3.1 rootful + rootless 编译、打包冒烟；真机复测由用户执行（重点：禁流生效判支持、系统对抗时重试、停充结果卡显示判定窗口电流）
