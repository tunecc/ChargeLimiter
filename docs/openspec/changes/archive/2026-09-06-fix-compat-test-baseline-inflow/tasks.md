# 任务清单：修复电池兼容性测试基线污染与禁流误判

## 1. 引擎修复

- [x] 1.1 基线相对判定：`beginTest` 记录基线信号（bCharging/bExtConnected/bCurrent），`handleSample` 禁流改为 `(bExtConnected && !extConnected) || (bCurrent >= 0 && current < 0)`，停充改为 `bCharging && !isCharging`；基线非充电先尝试恢复充电（≤10s）再复查；验证：代码复核 + 编译通过
- [x] 1.2 恢复充电基线阶段：`startWithSelection` 在 `enable=NO` 后轮询等待充电恢复（≤20s），失败 abort"无法恢复充电（可能已接近满电）"；验证：编译通过 + 代码复核

## 2. UI 与文案

- [x] 2.1 前置检查 charging 行不再硬阻断，改为"未在充电，开始时将自动恢复"橙色提示；说明区新增"请勿拔掉充电线"与"自动恢复充电基线"提示；验证：编译通过
- [x] 2.2 双语文案同步（en/zh-Hans 新增 key）；验证：plutil 校验 + 编译通过

## 3. 验证

- [x] 3.1 rootful + rootless 编译、打包冒烟完成；真机复测**显式记录为延期开放项**（用户自测清单：插电、电量中等：完整三项、已停充状态进入测试、禁流生效判定、不拔线）

## 开放验证项（OPEN）

- 真机复测（未执行）：同上清单，重点验证两个修复——已停充状态进入测试应能自动恢复充电基线后正常执行；禁流生效（不拔线）应判定"支持"而非误判。
